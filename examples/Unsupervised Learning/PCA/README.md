# PCA (Principal Component Analysis)

A linear dimensionality reduction technique.

## Mathematical Summary
Given a centered matrix $X$, we solve for the eigenvectors $v$ of the covariance matrix $\Sigma$:
$$\Sigma v = \lambda v$$
where $\lambda$ represents the variance captured by that component.